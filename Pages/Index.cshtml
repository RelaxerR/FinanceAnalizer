@page
@model IndexModel
@{
    ViewData["Title"] = "Главная";
}

<div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <div class="bg-white p-6 rounded-xl shadow-sm border-l-4 border-green-500">
        <h2 class="text-gray-500 uppercase text-xs font-bold">Общий доход</h2>
        <p class="text-3xl font-bold">150 000 ₽</p>
    </div>
    <div class="bg-white p-6 rounded-xl shadow-sm border-l-4 border-red-500">
        <h2 class="text-gray-500 uppercase text-xs font-bold">Общий расход</h2>
        <p class="text-3xl font-bold">85 000 ₽</p>
    </div>
</div>